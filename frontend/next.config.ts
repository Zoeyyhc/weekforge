import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the on-screen Next.js dev tools indicator (the floating "N" button).
  devIndicators: false,
};

export default nextConfig;
